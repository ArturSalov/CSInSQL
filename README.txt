# CSInSQL
Work with SQL (C#)
This project is designed to work with databases.
In the configuration file, the DB is displayed with which actions are taken.
The selected columns display information. Stools are grouped. 
If a numeric column is summarized according
